[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "customer-churn-classifier"
version = "0.1.0"
description = "A machine-learning project that predicts customer churn using classification models."
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pandas",
    "scikit-learn",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
    "pre-commit",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff.lint]
select = ["E", "F", "I"]